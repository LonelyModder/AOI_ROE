
character_events.35 = {
	type = character_event
	hidden = yes
	
	immediate = {
		if = {
			limit = {
				is_alive = yes
				exists = father
				father = {
					OR = {
						has_trait = nobilitas
					}
				}
			}
			if = {
				limit = {
					father = {
						has_trait = nobilitas
					}
					NOT = {
						has_trait = nobilitas
					}
				}
				add_trait = nobilitas
			}
			
		}
		if = {
			limit = {
				exists = mother
				mother = {
					has_trait = nobilitas
				}
				NOT = {
					has_trait = nobilitas
				}
			}
			add_trait = nobilitas	
		}
		
	}
}