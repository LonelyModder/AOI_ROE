namespace = bloodlines_setup_completion

bloodlines_setup_completion.1 = {
	type = country_event
	hidden = yes
	
	immediate = {
		every_character = {
			limit = {
				is_alive = yes
				OR = {
					exists = father
					exists = mother
				}
			}
			if = {
				limit = {
					exists = father
					father = {
						has_trait = nobilitas
					}
					NOT = {
						has_trait = nobilitas
					}
				}
				add_trait = nobilitas
			}
			
			if = {
				limit = {
					exists = mother
					mother = {
						has_trait = nobilitas
					}
					NOT = {
						has_trait = nobilitas
					}
				}
				add_trait = nobilitas	
			}
			
		}
	}
}