
pass_down_traits_effect = {
	scope:newborn = {
		if = {
			limit = {
				exists = father
				father = {
					OR = {
						has_trait = nobilitas
						has_trait = tarquinius
					}
				}
						
			}
			if = {
				limit = {
					father = {
						has_trait = nobilitas
					}
					NOT = {
						has_trait = nobilitas
					}
				}
				add_trait = nobilitas
			}
			if = {
				limit = {
					father = {
						has_trait = tarquinius
					}
					NOT = {
						has_trait = tarquinius
					}
				}
				add_trait = tarquinius
			}
		}
		if = {
			limit = {
				exists = mother
				mother = {
					has_trait = nobilitas
				}
				NOT = {
					has_trait = nobilitas
				}
			}
			add_trait = nobilitas
		}
		if = {
			limit = {
				exists = mother
				mother = {
					has_trait = tarquinius
				}
				NOT = {
					has_trait = tarquinius
				}
			}
			add_trait = tarquinius
		}
	}
}

bastard_major_bloodline_effect = {
	if = {
		limit = {
			scope:bastard_ruler = {
				has_trait = nobilitas
			}
		}
		add_trait = nobilitas
	}
	if = {
		limit = {
			scope:bastard_ruler = {
				has_trait = tarquinius
			}
		}
		add_trait = tarquinius
	}
}
